{
  "name": "@webapp-blueprint/web",
  "version": "0.0.0",
  "private": true,
  "description": "最小の縦切り（フロントエンド）。再帰的 features の配線を実証するだけの内容で、消して始めてよい（docs/plan/Template/20260807_template-design.md §9）",
  "type": "module",
  "exports": {
    ".": "./src/main.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && cp index.html dist/index.html",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
