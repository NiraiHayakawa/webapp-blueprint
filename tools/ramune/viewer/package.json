{
  "name": "@webapp-blueprint/ramune-viewer",
  "version": "0.0.0",
  "private": true,
  "description": ".ramune/graph.json を読み取り専用で表示するビューア。ramune の実行には一切関与しない（AGENTS.md §Stack索引）",
  "type": "module",
  "exports": {
    ".": "./src/main.ts"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@webapp-blueprint/ramune-graph": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
