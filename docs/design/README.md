# design

現行設計を置く場所。**「なぜ」ではなく「今どうなっているか」**だけを書く（原則 5「決定ログと現行規範の二層」の現行規範側）。

「なぜこの設計を選んだか」「他にどの選択肢を検討したか」は `docs/adr/` の役割であり、ここには書かない。書いてしまうと現行規範がもう一つの決定ログになり、二層モデルが崩れる。

このディレクトリの記述は追記ではなく**上書き**で更新する。設計が変わったら古い記述を消して今の形に置き換える。履歴は git log と `docs/adr/` が持つ。
