name: allowed
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    steps:
      - run: echo build
